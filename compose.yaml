services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_DEBUG: "True"
      DJANGO_ALLOWED_HOSTS: "localhost,127.0.0.1"
      DJANGO_DB_PATH: "/app/tutProject/data/db.sqlite3"
    volumes:
      - django_data:/app/tutProject/data

volumes:
  django_data:
